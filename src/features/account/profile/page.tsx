import { PortalPageHeading } from "@/components/portal/page-heading";
import { requireCustomer } from "@/server/auth/session";
import { updateProfile } from "../actions";
export default async function AccountProfilePage(){const {user}=await requireCustomer();return <><PortalPageHeading eyebrow="Account" title="Profile & preferences" description="Update your contact details and optional marketing consent. Transactional order messages remain enabled when needed to fulfil purchases."/><form action={updateProfile} className="mt-7 grid gap-6 lg:grid-cols-2"><Card title="Profile details"><Field label="First name" name="firstName" defaultValue={user.firstName??""}/><Field label="Last name" name="lastName" defaultValue={user.lastName??""}/><Field label="Email" value={user.email} disabled/><Field label="Phone" name="phone" type="tel" defaultValue={user.phone??""}/></Card><Card title="Communication preferences"><Check name="marketingEmailConsent" label="Product education and launch emails" defaultChecked={user.marketingEmailConsent}/><Check name="marketingWhatsappConsent" label="WhatsApp marketing" defaultChecked={user.marketingWhatsappConsent}/><Check label="Essential order updates" defaultChecked disabled/><button className="mt-6 min-h-11 rounded-full bg-mayera-espresso px-6 text-sm text-white">Save changes</button></Card></form></>}
function Card({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-[1.5rem] border border-mayera-line bg-mayera-paper p-6"><h2 className="font-serif text-2xl">{title}</h2><div className="mt-5 space-y-4">{children}</div></section>}
function Field({label,...props}:{label:string}&React.InputHTMLAttributes<HTMLInputElement>){return <label className="block text-xs font-medium uppercase tracking-[.1em]">{label}<input required={!props.disabled} className="mt-2 h-11 w-full rounded-xl border border-mayera-line bg-white px-3 text-sm normal-case tracking-normal disabled:bg-mayera-cream" {...props}/></label>}
function Check({label,...props}:{label:string}&React.InputHTMLAttributes<HTMLInputElement>){return <label className="flex items-start gap-3 text-sm text-mayera-espresso/68"><input type="checkbox" className="mt-1" {...props}/>{label}</label>}
